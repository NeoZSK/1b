def swap(T, i, j):
    (T[i], T[j]) = (T[j], T[i])

def sort_mint(T, n):
    for i in range(n):
        T[i]
        
        # swap(T, i, j) 
        
        # n -> 3
        # range(n) -> 0, 1, 2
        
sort_mint([1,2,3], 3)